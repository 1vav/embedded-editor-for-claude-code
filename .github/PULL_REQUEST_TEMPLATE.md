## What does this PR do?

## How to test

## Checklist

- [ ] `node scripts/smoke-stdio.mjs` passes
- [ ] `vendor/` rebuilt if `src/viewer/entry.jsx` was changed (`npm run build:viewer`)
- [ ] No breaking changes to existing MCP tool signatures (or major version bump if so)

## Related issues

Closes #
