node_modules/

# Built vendor bundles — generated by `npm run build` (or `prepublishOnly` before publish).
# Not tracked in git to avoid embedding third-party API keys from upstream packages.
# The npm release includes them via the `files` field in package.json.
vendor/

# Auto-generated runtime files (created when the viewer server runs)
.excalidraw-recent.json
.excalidraw-history/
*.log

# macOS
.DS_Store
.DS_Store?
._*

# Private Claude Code config
.claude/launch.json
.claude/settings.local.json

# Editor
.idea/
*.swp
*.swo

# Local dev / tooling
.playwright-mcp/
.todos-archive/
.mcp.json
todos/

# Git worktrees
.worktrees/

# Brainstorm mockups
.superpowers/
