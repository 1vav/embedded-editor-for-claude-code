npx lint-staged
npm run build:viewer:fast
# lowered from "high" to "critical" because duckdb's build-time tar dependency
# triggers high-severity npm audit warnings that are not runtime risks
npm audit --audit-level=critical
